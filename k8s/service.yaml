apiVersion: v1
kind: Service
metadata:
  name: autoroot-service
spec:
  type: NodePort
  selector:
    app: autoroot
  ports:
    - name: frontend
      port: 80
      targetPort: 3000
    - name: backend
      port: 8080
      targetPort: 8080